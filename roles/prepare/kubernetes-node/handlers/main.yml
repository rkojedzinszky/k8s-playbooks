---
- name: Apply kubernetes sysctls
  ansible.builtin.command: sysctl -p /etc/sysctl.d/kubernetes-node.conf

- name: Restart kubelet
  ansible.builtin.systemd:
    daemon_reload: yes
    name: kubelet
    state: restarted
